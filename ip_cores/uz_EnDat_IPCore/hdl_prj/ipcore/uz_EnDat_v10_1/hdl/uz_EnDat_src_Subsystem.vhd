-- -------------------------------------------------------------
-- 
-- File Name: C:\Users\ELSYS_PC\Documents\repos\ultrazohm_sw\ip_cores\uz_EnDat_IPCore\hdl_prj\hdlsrc\UZ_EnDat\uz_EnDat_src_Subsystem.vhd
-- Created: 2024-07-17 17:02:48
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: uz_EnDat_src_Subsystem
-- Source Path: UZ_EnDat/UZ_ENDAT/INITIALOFF/Subsystem
-- Hierarchy Level: 2
-- Model version: 3.8
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY uz_EnDat_src_Subsystem IS
  PORT( clk                               :   IN    std_logic;
        reset_x                           :   IN    std_logic;
        enb                               :   IN    std_logic;
        Trigger                           :   IN    std_logic;
        count                             :   OUT   std_logic_vector(15 DOWNTO 0)  -- uint16
        );
END uz_EnDat_src_Subsystem;


ARCHITECTURE rtl OF uz_EnDat_src_Subsystem IS

  -- Signals
  SIGNAL enb_gated                        : std_logic;
  SIGNAL count_step                       : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL count_from                       : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL Trigger_delayed                  : std_logic;
  SIGNAL HDL_Counter_out1                 : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL count_1                          : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL need_to_wrap                     : std_logic;
  SIGNAL count_value                      : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL HDL_Counter_out1_hold            : unsigned(15 DOWNTO 0);  -- uint16

BEGIN
  -- Count limited, Unsigned Counter
  --  initial value   = 0
  --  step value      = 1
  --  count to value  = 65535
  count_step <= to_unsigned(16#0001#, 16);

  count_from <= to_unsigned(16#0000#, 16);

  Trigger_delay_process: PROCESS (clk)
  BEGIN
    IF clk'event AND clk = '1' THEN
      IF reset_x = '1' THEN
        Trigger_delayed <= '0';
      ELSIF enb = '1' THEN
        Trigger_delayed <= Trigger;
      END IF;
    END IF;
  END PROCESS Trigger_delay_process;

  enb_gated <= ((NOT Trigger_delayed AND Trigger) OR (NOT Trigger AND Trigger_delayed)) AND enb;

  count_1 <= HDL_Counter_out1 + count_step;

  
  need_to_wrap <= '1' WHEN HDL_Counter_out1 = to_unsigned(16#FFFF#, 16) ELSE
      '0';

  
  count_value <= count_1 WHEN need_to_wrap = '0' ELSE
      count_from;

  HDL_Counter_process : PROCESS (clk)
  BEGIN
    IF clk'EVENT AND clk = '1' THEN
      IF reset_x = '1' THEN
        HDL_Counter_out1 <= to_unsigned(16#0000#, 16);
      ELSIF enb_gated = '1' THEN
        HDL_Counter_out1 <= count_value;
      END IF;
    END IF;
  END PROCESS HDL_Counter_process;


  count_hold_process : PROCESS (clk)
  BEGIN
    IF clk'EVENT AND clk = '1' THEN
      IF reset_x = '1' THEN
        HDL_Counter_out1_hold <= to_unsigned(16#0000#, 16);
      ELSIF enb_gated = '1' THEN
        HDL_Counter_out1_hold <= HDL_Counter_out1;
      END IF;
    END IF;
  END PROCESS count_hold_process;


  count <= std_logic_vector(HDL_Counter_out1_hold);

END rtl;

